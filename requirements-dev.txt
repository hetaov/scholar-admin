# 开发/测试/评估依赖 — 生产镜像不安装（Dockerfile 只装 requirements.txt）
# 本文件用于本地开发、CI 单元测试、L3 回归、周报批量评估。
# 安装：pip install -r requirements-dev.txt -r requirements.txt

# 测试框架
pytest>=8.0.0
pytest-asyncio>=0.23.0

# L3 批量评估（S4.1）：RAGAS 四指标（faithfulness/answer_relevancy/context_precision/context_recall）
# 重依赖（langchain 生态）；services/providers/batch_eval.py 延迟导入，
# 未安装时不影响服务主链路运行，仅 run_ragas_batch 不可用。
# 安装冒烟/失败登记见 会话训练评估重构执行计划.md §5 S4.1。
ragas>=0.4.0,<0.5

# L3 回归断言（S4.1-⑨，独立目录 tests/l3_regression，不进入默认 pytest 全量收集）
deepeval>=4.1.0,<5.0

# RAGAS LLM 走火山 OpenAI 兼容接口（langchain_openai.ChatOpenAI，base_url=VOLCANO_BASE_URL）
langchain-openai>=0.3.0

# 约束：ragas 0.4.x 依赖 langchain_community.chat_models.vertexai（0.4.x 已移除该模块），
# 重装环境须保持 langchain-community<0.4（2026-08-18 安装踩坑记录，见执行计划 §3 S4.1 阻塞登记）
langchain-community<0.4
