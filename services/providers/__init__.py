# Auto-generated package marker (code refactor shim compatibility).
# Required so services.<dir>.xxx resolves correctly as a regular subpackage
# (services/__init__.py is a regular package, so subdirs must be regular packages too).
# Leave empty except for this header.
