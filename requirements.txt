# 生产依赖 — Dockerfile 仅安装此文件。
# 开发/测试/评估依赖（pytest/ragas/deepeval/langchain）见 requirements-dev.txt。
fastapi==0.115.0
uvicorn==0.30.6
httpx==0.27.2
# pydantic 约束与 deepeval 4.1.x（要求 >=2.11.7）对齐；2.9.2 为陈旧锁定，全新环境解析冲突
pydantic>=2.11.7,<3.0.0
openai>=1.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
langgraph>=0.2.0
tencentcloud-sdk-python-asr>=3.1.137
# TTS 语音合成官方 SDK（F3/3.2，服务进程 .venv 必需；缺失时 /audio/tts 降级 TTS_UNAVAILABLE）
tencentcloud-sdk-python-tts>=3.1.101
# OCR 通用印刷体官方 SDK（F4.2，服务进程 .venv 必需；缺失/无凭据时扫描降级 ocr_status=failed）
tencentcloud-sdk-python-ocr>=3.1.101
# 腾讯云语音 SDK（SOE-N 新版，纯源码分发在 vendor/tencentcloud-speech-sdk-python）
# 唯一第三方依赖：websocket-client。**必须 <1.0**：SDK 的回调是 `on_close(ws)`（只收一个参数），
# 0.x 会先 inspect 回调形参个数（_get_close_args → 形参 !=3 则返回 []），1.0 起改为无条件传
# (ws, close_status_code, close_reason) → `TypeError: on_close() takes 1 positional argument
# but 3 were given`（2026-09-22 逐版本实测定位；此前靠改 vendor 源码兜底，已废除）
websocket-client>=0.48,<1.0

# E1.2 英语语句校验内存缓存（services/english/validation.py TTLCache，TTL 1h）
cachetools>=5.0.0

# F3.2 A4 练习纸渲染（ADR-0010：HTML/CSS → PDF+PNG）
# WeasyPrint 纯 Python HTML→PDF 引擎；Pillow 用于 PDF→PNG 截图
weasyprint>=60.0
Pillow>=10.0.0
# 家长核对二维码生成（纯 Python 生成 PNG/SVG，无系统依赖）
qrcode[pil]>=7.4.0
